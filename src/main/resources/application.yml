spring:
    datasource:
        username: root
        password: root
        url: jdbc:mysql://localhost:3306/blog?createDatabaseIfNotExist=true
    jpa:
        hibernate:
            ddl-auto: validate
